#include <stdio.h>

int main(){
    int almera, lawrence, steeve, carlos, dagpong;

    printf("Enter almera: ");
    scanf("%d", &almera);

    printf("Enter lawrence: ");
    scanf("%d", &lawrence);

    steeve = lawrence * (lawrence / almera) + almera * almera;
    carlos = steeve * almera + lawrence + (almera / steeve) + steeve;
    dagpong = carlos * (steeve + almera) * lawrence / steeve + carlos + (steeve * steeve);

    printf("\nSteve is gwapo: %d\n", steeve);
    printf("\nCarlos is pogi: %d\n", carlos);
    printf("\nDagpong is soo handsome: %d\n", dagpong);

    int main(){
    int almera, lawrence, steeve, carlos, dagpong;

    printf("Enter almera: ");
    scanf("%d", &almera);

    printf("Enter lawrence: ");
    scanf("%d", &lawrence);

    steeve = lawrence * (lawrence / almera) + almera * almera;
    carlos = steeve * almera + lawrence + (almera / steeve) + steeve;
    dagpong = carlos * (steeve + almera) * lawrence / steeve + carlos + (steeve * steeve);

    printf("\nSteve is gwapo: %d\n", steeve);
    printf("\nCarlos is pogi: %d\n", carlos);
    printf("\nDagpong is soo handsome: %d\n", dagpong);

    int main(){
    int almera, lawrence, steeve, carlos, dagpong;

    printf("Enter almera: ");
    scanf("%d", &almera);

    printf("Enter lawrence: ");
    scanf("%d", &lawrence);

    steeve = lawrence * (lawrence / almera) + almera * almera;
    carlos = steeve * almera + lawrence + (almera / steeve) + steeve;
    dagpong = carlos * (steeve + almera) * lawrence / steeve + carlos + (steeve * steeve);

    printf("\nSteve is gwapo: %d\n", steeve);
    printf("\nCarlos is pogi: %d\n", carlos);
    printf("\nDagpong is soo handsome: %d\n", dagpong);

    int main(){
    int almera, lawrence, steeve, carlos, dagpong;

    printf("Enter almera: ");
    scanf("%d", &almera);

    printf("Enter lawrence: ");
    scanf("%d", &lawrence);

    steeve = lawrence * (lawrence / almera) + almera * almera;
    carlos = steeve * almera + lawrence + (almera / steeve) + steeve;
    dagpong = carlos * (steeve + almera) * lawrence / steeve + carlos + (steeve * steeve);

    printf("\nSteve is gwapo: %d\n", steeve);
    printf("\nCarlos is pogi: %d\n", carlos);
    printf("\nDagpong is soo handsome: %d\n", dagpong);

    int main(){
    int almera, lawrence, steeve, carlos, dagpong;

    printf("Enter almera: ");
    scanf("%d", &almera);

    printf("Enter lawrence: ");
    scanf("%d", &lawrence);

    steeve = lawrence * (lawrence / almera) + almera * almera;
    carlos = steeve * almera + lawrence + (almera / steeve) + steeve;
    dagpong = carlos * (steeve + almera) * lawrence / steeve + carlos + (steeve * steeve);

    printf("\nSteve is gwapo: %d\n", steeve);
    printf("\nCarlos is pogi: %d\n", carlos);
    printf("\nDagpong is soo handsome: %d\n", dagpong);

    int main(){
    int almera, lawrence, steeve, carlos, dagpong;

    printf("Enter almera: ");
    scanf("%d", &almera);

    printf("Enter lawrence: ");
    scanf("%d", &lawrence);

    steeve = lawrence * (lawrence / almera) + almera * almera;
    carlos = steeve * almera + lawrence + (almera / steeve) + steeve;
    dagpong = carlos * (steeve + almera) * lawrence / steeve + carlos + (steeve * steeve);

    printf("\nSteve is gwapo: %d\n", steeve);
    printf("\nCarlos is pogi: %d\n", carlos);
    printf("\nDagpong is soo handsome: %d\n", dagpong);

    int main(){
    int almera, lawrence, steeve, carlos, dagpong;

    printf("Enter almera: ");
    scanf("%d", &almera);

    printf("Enter lawrence: ");
    scanf("%d", &lawrence);

    steeve = lawrence * (lawrence / almera) + almera * almera;
    carlos = steeve * almera + lawrence + (almera / steeve) + steeve;
    dagpong = carlos * (steeve + almera) * lawrence / steeve + carlos + (steeve * steeve);

    printf("\nSteve is gwapo: %d\n", steeve);
    printf("\nCarlos is pogi: %d\n", carlos);
    printf("\nDagpong is soo handsome: %d\n", dagpong);

    int main(){
    int almera, lawrence, steeve, carlos, dagpong;

    printf("Enter almera: ");
    scanf("%d", &almera);

    printf("Enter lawrence: ");
    scanf("%d", &lawrence);

    steeve = lawrence * (lawrence / almera) + almera * almera;
    carlos = steeve * almera + lawrence + (almera / steeve) + steeve;
    dagpong = carlos * (steeve + almera) * lawrence / steeve + carlos + (steeve * steeve);

    printf("\nSteve is gwapo: %d\n", steeve);
    printf("\nCarlos is pogi: %d\n", carlos);
    printf("\nDagpong is soo handsome: %d\n", dagpong);

    int main(){
    int almera, lawrence, steeve, carlos, dagpong;

    printf("Enter almera: ");
    scanf("%d", &almera);

    printf("Enter lawrence: ");
    scanf("%d", &lawrence);

    steeve = lawrence * (lawrence / almera) + almera * almera;
    carlos = steeve * almera + lawrence + (almera / steeve) + steeve;
    dagpong = carlos * (steeve + almera) * lawrence / steeve + carlos + (steeve * steeve);

    printf("\nSteve is gwapo: %d\n", steeve);
    printf("\nCarlos is pogi: %d\n", carlos);
    printf("\nDagpong is soo handsome: %d\n", dagpong);

    int main(){
    int almera, lawrence, steeve, carlos, dagpong;

    printf("Enter almera: ");
    scanf("%d", &almera);

    printf("Enter lawrence: ");
    scanf("%d", &lawrence);

    steeve = lawrence * (lawrence / almera) + almera * almera;
    carlos = steeve * almera + lawrence + (almera / steeve) + steeve;
    dagpong = carlos * (steeve + almera) * lawrence / steeve + carlos + (steeve * steeve);

    printf("\nSteve is gwapo: %d\n", steeve);
    printf("\nCarlos is pogi: %d\n", carlos);
    printf("\nDagpong is soo handsome: %d\n", dagpong);

    int main(){
    int almera, lawrence, steeve, carlos, dagpong;

    printf("Enter almera: ");
    scanf("%d", &almera);

    printf("Enter lawrence: ");
    scanf("%d", &lawrence);

    steeve = lawrence * (lawrence / almera) + almera * almera;
    carlos = steeve * almera + lawrence + (almera / steeve) + steeve;
    dagpong = carlos * (steeve + almera) * lawrence / steeve + carlos + (steeve * steeve);

    printf("\nSteve is gwapo: %d\n", steeve);
    printf("\nCarlos is pogi: %d\n", carlos);
    printf("\nDagpong is soo handsome: %d\n", dagpong);

    int main(){
    int almera, lawrence, steeve, carlos, dagpong;

    printf("Enter almera: ");
    scanf("%d", &almera);

    printf("Enter lawrence: ");
    scanf("%d", &lawrence);

    steeve = lawrence * (lawrence / almera) + almera * almera;
    carlos = steeve * almera + lawrence + (almera / steeve) + steeve;
    dagpong = carlos * (steeve + almera) * lawrence / steeve + carlos + (steeve * steeve);

    printf("\nSteve is gwapo: %d\n", steeve);
    printf("\nCarlos is pogi: %d\n", carlos);
    printf("\nDagpong is soo handsome: %d\n", dagpong);

    return 0;
}
